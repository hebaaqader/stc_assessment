package com.filemanager.service.interfaces;

public interface FileService {

    // File uploadFile(String spaceName, String folderName, String fileName, InputStream fileData, String userEmail, PermissionLevel permissionLevel);

    // InputStream downloadFile(String spaceName, String folderName, String fileName, String userEmail, PermissionLevel permissionLevel);

}
